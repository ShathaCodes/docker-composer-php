Environment for Symfony

docker-compose build incase it's your first time or you have changed either the Dockerfile and/or the docker-compose.yml
docker-compose up

docker ps to get the container id of your app
docker exec -it container-id /bin/bash

composer create-project symfony/skeleton app

 
